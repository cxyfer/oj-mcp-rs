{
  "name": "${node_pkg}",
  "version": "${node_version}",
  "description": "Platform-specific binary for oj-mcp-rs",
  "os": ["${node_os}"],
  "cpu": ["${node_arch}"],
  "files": ["bin/"],
  "repository": {
    "type": "git",
    "url": "https://github.com/cxyfer/oj-mcp-rs"
  }
}
